name: Production

on:
  push:
    branches:
      - production

permissions:
  actions: read
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:

      # descobrir BUILD_SHA
      # descobrir CI_RUN_ID
      # descobrir ARTIFACT_NAME

      # baixar artifact

      # deployment placeholder
